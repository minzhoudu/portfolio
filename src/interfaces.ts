export interface Technology {
  id: string;
  name: string;
  img: string;
  description: string;
}

export interface Work {
  title: string;
  img: string;
  description: string;
  url: string;
}
